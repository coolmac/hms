<div id="login" class="container animated fadeInDown">
  <div id="content" class="smart-form client-form">
    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-10" style="padding-top:10px">
      <div class="well no-padding">
        <header>
          Formatting basic questions of super category: <%=@super_category%>,  category: <%=@category%>
        </header>
          <%= form_tag vitals_examinations_path(:previous_category => @category, :previous_super_category => @super_category, :category => 'vitals', :super_category => 'examination') do %>
          <fieldset>
            <section>
            <%= label_tag 'description', 'Description' %>
            <%= text_area_tag :gpe, @current_visit.gpe, :autofocus => true, class: "form-control", cols:4, rows:2 %>
            </section>
            <%= render 'form' %>
          </fieldset>
          <% end %>
      </div>
    </div>
  </div>
</div>