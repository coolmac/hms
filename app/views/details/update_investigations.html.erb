<h1> top menu will have some basic patient details, we will make it part of the template itself </h1>

<h2> Formatting basic Investigations of category: <%=@category%>,  sub_category: <%=@sub_category%></h2>

</table>

<%= form_tag show_updated_investigations_path do %>

  <% @investigations.each do |dq| %>
    <%= label_tag dq.title%>
    <%= text_field_tag "#{APP_CONFIG['investigation_prefix']}#{dq.id}", get_selected_report("#{APP_CONFIG['investigation_prefix']}#{dq.id}") %>
    <br>

  <% end %>

<%= text_field_tag :email, "Your email address..." %>
<%= submit_tag "Enter To Win" %>
<% end %>

